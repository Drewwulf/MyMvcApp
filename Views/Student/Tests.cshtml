@model List<Test>

@foreach (var test in Model)
{
    <div class="mainTestDiv">
        <h2 style="color:whitesmoke;">@test.TestName</h2>
        <p style="color:whitesmoke;">@test.TestDescription</p>
        <a class="btn btn-dark">Пройти тест</a>
    </div>
}