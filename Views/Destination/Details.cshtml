@using Microsoft.AspNetCore.Mvc.ModelBinding
@model MyMvcApp.Models.PlaceViewModel
<section class="section">
    <div class="mainDiv">
        <div class="maindiv2">
            <h2 class="h2white">Назва: @Model.Name </h2>
            <p>Адреса:@Model.Address</p>
        </div>
        <a asp-controller="Destination" asp-action="Create" class="btn btn-dark">Повернутися назад</a>
           
    </div>
</section>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}